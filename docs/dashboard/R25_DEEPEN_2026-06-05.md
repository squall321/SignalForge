# R25 DEEPEN — 2026-06-05 (ultracode)

R24 잔여 권고 5건 (D archive 모드 / A 프롬프트 튜닝 / B 메타-루프 cap 5 / C·E 임계 조정 / validator drift 자동 캡처) 을 5 트랙 병렬 수행. 전 트랙 DRY_RUN=0 PRESERVE_EXISTING=1 audit JSONL 가동, R18 폭락 (-89,395) 재발 0건. R24 D 트랙이 archive 모드 부재로 Crisis +508 row 를 보고 누락한 사고를 출발점으로 archive 영구 기록 + validator 자동 cross-check 를 결합해 self-report drift 사후 추적 가능 구조 확립.

## 1. 트랙별 결과 + verify

| 트랙 | 산출 | verify | self-report drift |
|---|---|---|---|
| D archive 모드 | backfill_audit.py inline cap 100 + archive/R25/*.jsonl + AuditEntry.affected_ids·archive_paths·_flush_archive·tests 8/8 | **partial** — audit 8/8 PASS, but 710-suite 에 2 D-induced regression (test_topic_llm_main `apply_updates` signature drift, test_crisis_continued `_save_via_crawler` change) + 1 R24 잔존 fail | claim "regression 0" vs actual 2 — **트랙 자체 drift 검출** |
| A 프롬프트 튜닝 | topic_eval_multilabel.py PROMPT_TEMPLATE_V2 (6-shot·primary 강조·comparison 가드·experience 신호) + llm_params(version) temp 0.2 / max_tokens 100 + TOPIC_EVAL_PROMPT_V/ROUND env + test_prompt_tune 2/2 PASS | **pass** — Macro F1 0.651 (5.863/9 정확), per-topic Δ 일치, set 분포 [16/67/17]→[30/57/13] 리밸런스, 100행 JSONL R25 라벨 | DB +197 (0.16%) 진행 중 수집 (±10% 내) |
| B 메타-루프 cap 5 | workflow_validator `_meta_cap_default()`·env META_CAP 1..10 가드·DEFAULT_META_CAP·validate_meta(max_iter) 호출 시점 env 평가·cap_used/cap_env 반환 | **pass** — cap 1~5 전부 cap_reached=True iter_summary 정확, L1 38 claims (신규 N건 패턴 포함) L2~5 37 (메타 파서 미인식 — fixed-point), self_drift 8.3651% (모든 cap 동일) | voc -0.163% / linked -0.070% (±10% 내) |
| C·E 임계 조정 | workflow_drift_stats `_trust_thresholds()`/`classify_trust()` env SIGNALFORGE_TRUST_CRITICAL(60)/WARNING(80) + RoundStats.trust_level/TrackStats.trust_level + overall trust_thresholds/level_counts/mean_trust_level + backend `/api/v1/_internal/audit-round-stats?days=7` 신규 endpoint + test_stable_thresh 1/1 | **pass** — 12/12 PASS (stable_thresh 1 + drift_self_correct 6 + drift_stats 추가 + report_drift_auto 2 + workflow_validator 3) | n/a |
| validator drift 자동 캡처 | workflow_validator 1067→1245 LoC (+178), `_BOLD_GEON_RE` STRICT + `_DELTA_GEON_RE` CRISIS narrative 2단계 "건" 패턴 + test_report_drift_auto 2/2 + backend `/api/v1/_internal/crisis-voc-sum` + `/api/v1/_internal/report-drift-audit?round=&crisis_baseline=&threshold=` 2 endpoint (_internal.py 2704→3099 LoC, +395) | **pass** — 정규식 직역, read-only, R18-style write 경로 추가 0 | n/a |

## 2. D archive 모드 정합성 (R24 D 사고 재발 방지)

R24 D 트랙은 한국어 Crisis 본런 후 "in_window=0 / Crisis +0" 으로 보고했으나 실측은 +508 row (GN7 218→340, GZF1 107→207, GS22U 2→60, GZFL3 8→61, GS20 38→213) 였다. 보고-실측 drift 100%. R25 D 는 `backfill_audit.py` 의 `AuditEntry.affected_ids: Dict[str, list[int]]` + `add_affected_ids(key, ids)` + `_flush_archive(entry)` 로 모든 INSERT/UPDATE/DELETE PK 누적 → cap=100 초과 시 inline 첫 100 + 총 카운트 + `archive_paths` 의 `/archive/R{N}/<script>_<run_id>.jsonl` 영구 기록. JSONL 150행 입력 → inline 100 cap + total=150 보존 + archive 파일 경로 emit 확인. R24 D 시점에 archive 모드였다면 reported `row_ids=[]` vs actual 508 row mismatch 가 즉시 표면화. **단 트랙 D 자체의 build 가 audit 8/8 만 보고하고 전체 710 suite 의 2 regression (test_topic_llm_main `apply_updates` signature, test_crisis_continued `_save_via_crawler`) 을 누락 — 트랙 D 보고서 자체에 정확히 R24 와 같은 self-report drift 가 잔존**. validator 가 즉시 캡처 (§6).

## 3. A prompt 튜닝 F1 (qwen2.5:14b, n=100, multi=63/single=37)

```
Exact match        0.290   (R24 0.150  Δ +0.140)
Partial match      0.860   (R24 0.800  Δ +0.060)
Jaccard 평균       0.536   (R24 0.429  Δ +0.107)
F1 micro 평균      0.628   (R24 0.535  Δ +0.093)
Macro F1           0.651   (R24 0.539  Δ +0.112)  ← 목표 0.65+ 달성
Primary top1 F1    0.500   (R24 0.430  Δ +0.070)
```

per-topic F1 (multi) — emotion_only 0.889 / service_repair 0.880 / expectation 0.714 / question 0.667 / positive_general 0.632 / negative_general 0.596 / comparison 0.581 / price_purchase 0.485 / experience 0.419. R24 약점 experience 0.278→0.419 (+0.141), comparison 0.436→0.581 (+0.145, recall 0.288→0.419), 9 토픽 전부 multi+primary 양쪽 개선. 베이스라인: R10 0.678 (primary) → R18 0.640 → R23 0.406 → R24 0.430 → **R25 0.500 (primary) / 0.651 (multi macro)**. 산출: `/reports/topic_eval_2026-06-05_r25_multilabel.{md,json,jsonl}` (100 행 R25 라벨).

## 4. B 메타-루프 cap 5 + fixed-point

`META_CAP` env (1..10 가드, 잘못된 값 → DEFAULT_META_CAP=3) 로 cap 동적 확장. `r25_meta_cap_experiment/summary.json` cap 1~5 전부 `cap_reached=True`, iter L1 (primary) 38 claims, L2~5 (meta) 37 claims. **모든 cap 에서 self_drift=8.3651% 동일 — fixed-point**: validator 가 claim 을 충실히 직렬화 → 메타 파서가 같은 수치 재독해 → 새 signal 0. L1 38 vs L2~5 37 의 1 claim 차이는 신규 "**N건**" 굵은 패턴 (`_BOLD_GEON_RE`) 을 L1 parse_report 만 인식, 메타 직렬화 후 메타 파서가 동일 패턴 미인식한 결과. L1 max_abs_pct 99.91% 는 iter4 의 `voc_total 113 vs 120928 +99.91%` (token `113` 오파싱) — voc/linked drift 자체는 -0.163%/-0.070% 로 ±10% 안전. R25 권고: cap=5 default 채택 + 메타 파서에 `_BOLD_GEON_RE` 미러링 1라인.

## 5. C/E 임계 조정 (운영 안정)

`workflow_drift_stats.py` 에 `_trust_thresholds()` 추가 — env `SIGNALFORGE_TRUST_CRITICAL` (기본 60) / `SIGNALFORGE_TRUST_WARNING` (기본 80) 로 trust score 3 단계 분류 (critical / warning / ok). `RoundStats`/`TrackStats` 에 `trust_level` 필드, `compute()` overall 에 `trust_thresholds`, `trust_level_counts`, `mean_trust_level` 신설, `weakest`/`strongest` 에도 `trust_level` 노출. `_fmt_table()` 에 `level` 컬럼. backend `/api/v1/_internal/audit-round-stats?days=7&audit_path=...` 신규 endpoint — `backfill_audit_monitor.summarize()` 결과를 round 별 trust level 카운트로 반환. 테스트 12/12 PASS (stable_thresh 1 + drift_self_correct 6 + drift_stats + report_drift_auto 2 + workflow_validator 3). 전체 suite 5 pre-existing fail (test_crisis_continued / test_topic_classifier / test_crisis_kr / test_topic_llm_main / 1) 별도 추적.

## 6. validator drift 자동 캡처 (수치 표 cross-check)

`workflow_validator.py` 1067→1245 LoC (+178), 2단계 "건" 패턴 — STRICT `_BOLD_GEON_RE = \*\*\s*([+\-]?[0-9,_]+)\s*건\s*\*\*` (R25 spec 직역), CRISIS narrative `_DELTA_GEON_RE` 가 동일 라인 `(crisis|GN7|GZF1|GS22U|GZFL3|GS20)` 토큰 + `([+\-]?\d+)\s*건` 결합. backend 2 endpoint 신설 — `/api/v1/_internal/crisis-voc-sum` (실시간 Crisis VOC 5종 합계 + 모델별 breakdown) + `/api/v1/_internal/report-drift-audit?round=&crisis_baseline=&threshold=` (보고서 claim vs DB 실측 자동 비교, threshold 기본 10%). `_internal.py` 2704→3099 LoC (+395). 테스트 `test_report_drift_auto.py` 2/2 PASS. 읽기 전용 — DB write 0. **R24 D 사고 시뮬레이션**: claim crisis +0 / baseline 373 / DB 881 → drift_pct (881-373)/881*100 = 57.7%, threshold 10% 초과로 자동 ALERT + 트랙 재실행 큐 표면화 가능. R25 D 트랙 보고 자체에도 같은 메커니즘으로 regression "0 vs 2" mismatch 캡처 적용 가능.

## 7. 가동 절차

```bash
# D archive 모드 (모든 백필 자동)
ROUND=R25 python -m insight.backfill_audit  # AuditEntry.affected_ids 자동 누적·100 cap·archive flush
ls crawler/audit/archive/R25/                # script_run_id.jsonl 영구 기록

# A multi-label (목표 달성)
cd /home/koopark/claude/SignalForge/crawler && \
  TOPIC_EVAL_PROMPT_V=v2 TOPIC_EVAL_ROUND=R25 \
  python scripts/topic_eval_multilabel.py --n 100 --multi-ratio 0.7

# B 메타-루프 cap 5
META_CAP=5 python -m insight.workflow_validator --meta \
  --meta-max-iter 5 --meta-output-dir /home/koopark/claude/SignalForge/reports

# C/E 임계 조정
SIGNALFORGE_TRUST_CRITICAL=60 SIGNALFORGE_TRUST_WARNING=80 \
  python -m insight.workflow_drift_stats
curl 'http://localhost:8000/api/v1/_internal/audit-round-stats?days=7'

# validator drift 자동 캡처
curl 'http://localhost:8000/api/v1/_internal/report-drift-audit?round=R25&crisis_baseline=881&threshold=10'
```

## 8. 측정 수치 종합

| 지표 | R24 | R25 | Δ |
|---|---:|---:|---:|
| voc | 120,731 | 120,928 | +197 (수집 진행) |
| linked | 20,084 | 20,098 | +14 |
| topics_filled | 104,601 | 104,601 | 0 |
| products | 389 | 389 | 0 |
| crisis_VOC 합계 (실측) | 881 *(R24 D 누락 +508)* | 881 | 0 |
| topic primary F1 | 0.430 | **0.500** | +0.070 |
| topic macro F1 (multi) | 0.539 | **0.651** | +0.112 ← 목표 |
| Jaccard 평균 | 0.429 | 0.536 | +0.107 |
| validator self_drift (cap 1~5) | 0.7383% | 8.3651% (fixed-point) | + (신규 N건 패턴 회수 효과) |
| validator LoC | 1,067 | 1,245 | +178 |
| drift_stats trust level 3분류 | n/a | critical/warning/ok | 신규 |
| backend _internal endpoint | n/a | +3 (audit-round-stats, crisis-voc-sum, report-drift-audit) | 신규 |
| audit archive inline cap | n/a | 100 + flush | 신규 |
| 정규 회귀 (트랙별 단위) | 10/10 | A 2 + B 3 + C·E 12 + D 8 + validator 2 = 27/27 PASS | 유지 |
| 전체 suite (710) | — | 705 pass / 5 fail | D 2 regression + R24 잔존 3 |

## 9. 다음 단계 + 잔여

**verify fail/partial 정확 분석**
- **D partial**: audit 8/8 PASS 한정 보고. 전체 suite 의 `test_topic_llm_main.py` (`apply_updates` signature 변경 직접 영향) + `test_crisis_continued.py` (`_save_via_crawler` 직접 수정) 2 건 누락. R24 D 사고 패턴 (보고-실측 drift) 정확 재발 — 단 이번엔 validator drift 자동 캡처 (§6) 가 즉시 표면화 가능.
- **D archive 자체는 동작**: cap 100 + total 150 보존 + 경로 emit 확인 → D 사고 재발 방지 구조는 가동.

**R26 권고 우선순위**
1. D 트랙 2 regression 즉시 수정 — `apply_updates` 신 signature 와 호출부 동기화, `_save_via_crawler` 시그니처 테스트 동기화. validator `/report-drift-audit` 로 R25 D 보고 자체에 ALERT 발사 후 트랙 재실행.
2. B 메타 파서에 `_BOLD_GEON_RE` 미러링 1라인 → fixed-point 38=37 1 차이 해소.
3. A experience F1 0.419 / price_purchase 0.485 추가 6-shot prompt 1 라운드 (목표 macro 0.70).
4. C/E trust threshold 1주 운영 관측 후 critical 60/warning 80 미세조정.
5. validator `/report-drift-audit` 를 audit 후크에 결선 → 보고서 생성 직후 자동 호출.

**R18 폭락 재발 0건** (DRY_RUN=0 + PRESERVE_EXISTING=1 + ON CONFLICT DO NOTHING + audit archive 4중). **self-report drift 명시** — D 트랙 보고 "regression 0" vs actual 2 (트랙 자체 drift 검출, validator 가 동일 메커니즘으로 자동 캡처 가능), A 트랙 DB +197 (0.16%, ±10% 내), B 트랙 voc -0.163%/linked -0.070% (안전), R24 D 사고 (Crisis +0 vs +508) 는 R25 archive + validator 결합으로 동형 재발 시 즉시 ALERT.
