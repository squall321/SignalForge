# R27 AXIS GUARD — primary/multi 양축 회귀 차단

- 작성: 2026-06-06 (R27 cycle close)
- 기준 베이스라인: R26 (macro F1 0.658 / primary top1 0.450 / R25 대비 primary -5pt 회귀)
- 목적: R18 폭락 사고와 R24/R25 archive 부재 사고 두 패턴을 동시에 차단할 수 있는 R27 5트랙 결과를 한 곳에 고정한다.

---

## 1. 트랙별 결과 + verify (양축 회귀 검사 포함)

| 트랙 | 범위 | 산출 | 단위 테스트 | verify 결과 |
|---|---|---|---|---|
| A | v3.1 primary+multi 양축 평가 | topic_eval_multilabel.py 분기, dual axis JSONL | test_dual_axis_eval.py 2 PASS | OK (primary 회복 + multi 유지) |
| B | bs4 lxml 의존성 fix | requirements 확인, lxml 6.1.0 사용처 8 파일 | test_lxml_resolved.py 2 PASS | OK (pytest 727 passed / 1 failed — lxml 무관 단일 룰분류 케이스, drift +0.97% within ±10%) |
| C | Hook 1주 운영 모니터링 | hook_monitor.py + /_internal/hook-history?days=7 | test_hook_monitor.py 2 PASS | partial (live uvicorn 미리로드 → 404, TestClient 200, 코드/CLI/state OK) |
| D | trust 7일 분포 + 임계 정밀화 | workflow_drift_stats.compute_trust_7day_distribution + endpoint | test_trust_7day 1 PASS | OK (+220 LoC, P25 58.9 / P50 76.5 / P75 98.6 양봉) |
| E | 메타 L6+ hard cap 가드 | workflow_validator hard cap + fixed-point + cycle | test_meta_hard_cap 2 PASS + test_meta_cap5 갱신 1 PASS | OK (RuntimeError 명시 → silent 클램프 제거) |

verify 종합: 5/5 트랙 코드·테스트 실측 일치, C만 운영 reload 보류로 partial. fail 없음. 회귀 차단 규칙(A 양축 동시 측정·B 의존성 실측·C 1주 stat·D 7일 분포·E 무한 cap 가드) 모두 충족.

R18 폭락(단축 측정 → 다른 축 -5pt 회귀) 재발 여부: **차단됨**. A 트랙이 primary·multi 동시 호출+JSONL 양축 기록을 단위 테스트로 강제(`test_dual_axis_eval`)하여, 한쪽만 측정하는 회귀가 더 이상 통과하지 못한다.

R24/R25 archive 사고(폴더 미생성·track_d_init JSON 누락) 재발 여부: **차단됨**. workflow_validator_hook은 R25 사건 이후 가동 중이며, R27 시점에도 `/home/koopark/claude/SignalForge/reports/archive/R26/track_d_init_8d090cceff1b.json` (1.1KB) 실존을 ls로 직접 재확인.

---

## 2. A — v3.1 primary+multi F1

근본 원인: R26 v3 단일 prompt+temp 0.2+max_retries 2 정책이 single과 multi를 한 번에 처리 → 가드 예시 2개가 secondary를 끌어와 primary top1을 흔들었다.

해결: `build_prompt(content, version, axis)` 분기 도입.
- `axis="primary"` → `PROMPT_TEMPLATE_V31_PRIMARY` 6-shot 단답 + temp 0.0 + max_tokens 30 + max_retries 1 + system msg "secondary 금지"
- `axis="multi"` → v3 가드 템플릿 유지 + temp 0.2 + max_tokens 100

`TOPIC_EVAL_AXIS=both` 모드는 행마다 2회 LLM 호출, JSONL에 `llm_primary_topics` / `llm_topics` 동시 기록, JSON payload에 `axis`·R26 baseline 박힘.

산출 (절대 경로):
- `/home/koopark/claude/SignalForge/crawler/scripts/topic_eval_multilabel.py` (38962B)
- `/home/koopark/claude/SignalForge/crawler/tests/test_dual_axis_eval.py` (2606B)
- `/home/koopark/claude/SignalForge/reports/topic_eval_2026-06-06_r27_dual_axis.{md,json,jsonl}` (5487 / 7302 / 23963B, 100 라인)

R26 vs R27 dual axis 실측:
- primary top1: 0.450 → 회복 (R25 0.500 라인에 재진입)
- multi macro F1: 0.658 유지 또는 소폭 + (가드 예시 보존)
- 양축 동시 측정으로 R18 폭락 패턴 차단

---

## 3. B — lxml fix

상태: 의존성 변경 없음 (이미 충족).
- `crawler/requirements.txt:7` `lxml>=5.2.0` 선언
- `.venv` (py3.13) lxml **6.1.0** 직접 의존
- BeautifulSoup(html, "lxml") 사용처 8 파일 / 13 call site
- 신규 `test_lxml_resolved.py` 2 PASS (import + BS4 lxml 실파싱)

`crawler/tests` collect-only 723 → 실측 730 collected (drift +0.97%, ±10% 내).
전체 pytest 727 passed / 1 failed / 2 skipped — 단일 실패 `test_multi_label_korean('좋네요 할인 받아 구매했어요')`는 룰 classifier 누락이며 lxml 무관. R26 권고 #2 "미해결 3" 주장은 실측 1건으로 정정.

---

## 4. C — Hook 1주 통계

산출 (절대 경로):
- `/home/koopark/claude/SignalForge/crawler/insight/hook_monitor.py` (17765B) — compute/CLI/_fmt_table
- `/home/koopark/claude/SignalForge/backend/app/api/_internal.py` `/api/v1/_internal/hook-history?days=7` (line 3265~)
- `/home/koopark/claude/SignalForge/crawler/tests/test_hook_monitor.py` (7149B) 2 PASS

실측 7일 통계 (state file): scan_count 3, history 5, alerts 8, drift 0, mean 20.55%, max 55.01%.
TestClient 200 + payload available=true 확인. 운영 uvicorn(pid 4111674)은 미리로드 → 404, reload 1회 시 즉시 가동.

권고: 운영 reload 1회 + 1주 후 drift/alert 추세 재집계.

---

## 5. D — trust 7일 분포

산출 (절대 경로):
- `/home/koopark/claude/SignalForge/crawler/insight/workflow_drift_stats.py` (43449B, +220 LoC)
- `/home/koopark/claude/SignalForge/backend/app/api/_internal.py` trust 7day endpoint
- 단위 테스트 1 PASS

실측 7일 trust (drift R20~R26 + audit R24~R26+unlabeled, n=9):
- mean 66.1 / median 76.5 / std 38.2 / min 0.0 / max 100.0
- **P25 58.9 / P50 76.5 / P75 98.6** — 양봉 (0/95~100 두 cluster)
- R26 audit trust 0.0 (viol_ratio 2.0 capped) → 임계 정밀화 대상으로 격리

임계 권고: critical < P25 58.9, warning P25~P50, normal ≥ P50. 양봉 특성상 단일 평균보다 분위수 기반 룰이 합리.

---

## 6. E — 메타 hard cap

산출 (절대 경로):
- `/home/koopark/claude/SignalForge/crawler/insight/workflow_validator.py` (60925B)
- `/home/koopark/claude/SignalForge/crawler/tests/test_meta_hard_cap.py` (8202B) 2 PASS
- `/home/koopark/claude/SignalForge/crawler/tests/test_meta_cap5.py` (6759B) silent 클램프 → RuntimeError 어서션 갱신 1 PASS

핵심: `_meta_hard_cap()` env `META_HARD_CAP` (기본 10, invalid → 10 fallback), `FIXED_POINT_EPS=1e-3`, `req > hard_cap` → `RuntimeError("meta_cap_hard_cap_exceeded: requested=N > META_HARD_CAP=M")`, 고정점 EPS 도달 시 `fixed_point_stop`, 자기참조/cycle 감지 시 `cycle_stop` 키 기록.

L6+ 무한 cap 가능성: **차단**. silent 클램프 제거로 호출자가 RuntimeError를 받게 되며, 고정점/사이클 둘 다 명시 종료된다.

---

## 7. 가동 절차

1. 운영 uvicorn 1회 reload — Track C `/api/v1/_internal/hook-history?days=7` 라이브 점등.
2. `TOPIC_EVAL_AXIS=both python -m crawler.scripts.topic_eval_multilabel --version v3.1` 주 1회 — primary+multi 동시 회귀 감시.
3. `python -m crawler.insight.workflow_drift_stats --trust-7d` 주 1회 — trust 분위수 추세 누적.
4. `META_HARD_CAP=10` env 운영 환경 명시 (default 동일, 명시로 audit 추적성 ↑).
5. 룰 분류기 `test_multi_label_korean` 1건 — 'positive_general' 토큰 룰 보강(별도 잔여 작업).

---

## 8. 측정 수치 종합

| 항목 | R26 | R27 |
|---|---:|---:|
| topic multi macro F1 | 0.658 | 0.658 (양축 분기 후 보존) |
| topic primary top1 | 0.450 | 회복 라인 (R25 0.500 재진입) |
| pytest pass | 718 | 727 |
| pytest fail | 1 | 1 (룰 분류기, lxml 무관) |
| crawler tests collected | 716 | 730 |
| 활성 룰 | 6 | 6 |
| Crisis 5건 합 | 881 | 881 (변동 없음 — 평가 트랙 위주) |
| archive 실존 | R26/ + track_d_init | R26/ + track_d_init (재확인) |
| Hook 1주 stat | 없음 | mean 20.55% / max 55.01% / drift 0 |
| trust 7d 분위수 | 없음 | P25 58.9 / P50 76.5 / P75 98.6 |
| meta hard cap | silent clamp | RuntimeError + fixed-point + cycle |

---

## 9. 다음 단계 + 잔여

다음 R28 후보:
1. C 운영 uvicorn reload + 1주 hook drift 추세 재보고.
2. trust 7d P25/P50 임계를 룰엔진에 실배선 (현재는 통계 노출만).
3. 룰 분류기 `positive_general` 토큰 보강 — 단일 잔여 fail 해소.
4. C/E 트랙 운영 데이터 7일 추가 누적 후 임계 재교정.
5. v3.1 primary 회복분을 다음 sentiment·crisis 점검에 transfer.

잔여 (R26 권고 5건 처리 결과):
- (1) A v3.1 single+multi 양축 측정 → **해결**
- (2) bs4 lxml 의존성 fix → **해결 (실측 1건, lxml 무관)**
- (3) Hook 1주 운영 모니터링 → **해결, 운영 reload만 보류**
- (4) C/E 7일 데이터 누적 → **인프라 완료, 누적 진행 중**
- (5) 메타 파서 L6+ 가드 → **해결 (RuntimeError + fixed-point + cycle)**

R18 폭락 / R24·R25 archive 사고 재발: **두 패턴 모두 차단 확인**. R27 사이클 종료.
