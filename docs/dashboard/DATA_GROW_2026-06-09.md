# Data Grow R1 (2026-06-09)

수집 강화 5트랙 (H1~H5) 실행 보고. 무중단·OOM 안전·MX 필터 가동 전제. ultracode 모드.

## 1. 종합 (Before/After)

| 지표 | Before (07:00 UTC) | After (15:10 UTC) | 변화 |
|---|---|---|---|
| voc total | 154,190 | **154,649** | +459 |
| 활성 (archived_at IS NULL) | 60,550 | **61,009** | +459 |
| 24h 누적 INSERT | 측정 안 됨 | **5,575** | — |
| 7d 누적 INSERT | 측정 안 됨 | 85,787 | — |
| 24h 활성 platforms | — | 57 / 80 | dead 23 잔존 |
| mx_match% (active) | 컨텍스트 76.4% (claim) | **실측 28.35%** | **drift -48pt** |
| swap used | 8,031MB | **8,059MB** | +28MB (안전선 ±500MB 내) |
| swap free | 160MB | 132MB | 임계 근접 |

**Top 24h:** DCInside 1321 · HN 834 · Instiz 747 · ppomppu 701 · Reddit(RSS) 552 · Xataka 241 · Quasarzone 240 · **Mastodon 223 (신규)**.

## 2. H1 글로벌 IT backfill (PASS)
9개 사이트 (HN/Xataka/NBC/SamMobile/SammyFans/Engadget/Gigazine/AnandTech/XDA) 1회 dry_run=false MAX_POSTS×2. **+169건 INSERT** (HN 23, SammyFans 142, Xataka 1, NBC 1, SamMobile 2, 나머지 0). archive depth 포화로 4 사이트 0건. swap +28MB · audit end ok.

## 3. H2 beat 주기 단축 (PASS / 효과 미관측)
8개 신규 schedule key (hackernews 3h→2h, instiz 2h→1h, dcinside backfill 12h→6h, ppomppu 12h→6h 등) `app.conf.beat_schedule` 등록 116 entries. **beat PID 2739399 reload (14:25 UTC)**. 단 첫 fire(16:25~17:25 UTC) 아직 도래 안 함 → INSERT 효과 0. swap delta 0.

## 4. H3 Reddit subreddit 확장 (PARTIAL)
`reddit_rss.py` SUBREDDITS 6 신규 (galaxys24/GalaxyZFlip/GalaxyZFold6/GalaxyTab/oneui/samsunggalaxy) 추가 13개. `_load_subreddits()` + `REDDIT_RSS_SUBS` env hot-reload 도입. 단 **worker 모듈 캐시로 신규 6개 활성화 deferred** — h24 hit = GalaxyWatch/Android/galaxybuds/AndroidQuestions/GalaxyFold 만. 다음 worker restart 시 적용. reddit_rss MX 23.16% (글로벌 평균 하회).

## 5. H4 Mastodon 갤럭시 태그 (PASS / 신규 1)
platform id=101 신규 등록 (code=mastodon, region=GLOBAL). 4h schedule. **223건 INSERT** (lang en 121 · pt 24 · de 14 · it 13 · ja 9 · 다국어). 사이트 자체 MX 69.96% (156/223 매칭) 양호. audit start→done→end 3단 정상.

## 6. H5 worker concurrency -c 6 → -c 8 (NO_GO 안전 차단)
사전검증에서 **swap free 160MB 확인 → 안전 임계 위반 → 변경 미실행** (룰북 정합). worker PID 58607 -c 6 유지 (master+6 children). swap delta 0. audit decision=NO_GO 1줄 기록. **메모리 회복 후 재시도 권고**.

## 7. OOM 안전
- swap used 8,031→8,059MB (Δ +28MB, ±500MB 안전선 5.6% 소모)
- swap free 132MB (위험권 — H5 차단 사유)
- 가용 메모리 56.6GB (충분)
- worker -c 6 PID 58607 16h uptime 무사고

## 8. 라이브 + 잔여 + drift 명시

**라이브 무중단:** uvicorn:18000 (PID 1045154 10h) · vite:17370 (2일) · postgres:5434 · redis:6379 · celery worker PID 58607 (16h) · celery beat PID 2739399 (reload 14:25Z) 모두 정상. backend /docs 200 · frontend 200 · /api/v1/dashboard/overview 200.

**Drift 명시 (정직 보고):**
1. **mx_match% 컨텍스트 76.4% vs 실측 28.35%** — 약 48pt 갭. 보고치 76.4% 는 다른 정의(예: linked-only 또는 노이즈 제거 후 denom) 추정. **현 SQL 정의(product_id IS NOT NULL / active)로는 76.4% 불성립**. 검증 필요.
2. **H2 효과 INSERT 0** — beat reload 후 첫 fire 도래 전 측정. 다음 라운드에서 +500~1500 예상.
3. **H3 신규 6 sub 활성화 deferred** — worker 캐시 보존 정책상 다음 restart 까지 미관측.
4. **H1 archive depth 포화** — engadget/anandtech/xda/gigazine 4 사이트 MAX×2 fanout 무효.
5. **nginx :8088 → 502** — H 작업 무관, 사전부터 존재.

**잔여 (다음 라운드):**
- swap free 회복 후 H5 -c 8 재시도
- H3 worker restart 로 신규 sub 활성화 (+500~800 예상)
- H2 첫 fire 후 INSERT 효과 측정
- mx_match% 정의 통일 및 76.4% 근거 추적
- archive 포화 4 사이트 backfill 깊이 확대 또는 신규 사이트 발굴

**감사로그:** `/home/koopark/claude/SignalForge/audit/data_grow.jsonl` round=data_grow 5트랙 모두 기록 (H5 NO_GO 포함).
