# Data Clean 4 종합 보고 (2026-06-09)

## 1. 종합 (Before/After)

| 지표 | Clean 3 직후 | Clean 4 라이브 (04:28 UTC) | 델타 |
|---|---|---|---|
| voc total | 153,059 | **153,059** | 0 (수집 정지 윈도우) |
| 활성 (archived_at IS NULL) | 65,987 | **61,309** | **-4,678** |
| archive | 87,072 | **91,750** | **+4,678** |
| archive 비율 | 56.9% | **59.94%** | +3.04pt |
| mx_match_pct (endpoint 기준, 활성) | 69.2% | **74.4%** | **+5.2pt** |
| mx_rich_pct (활성) | 46.9% | 50.5% | +3.6pt |
| mx_match_pct (DB 직검 literal pattern) | 64.87% | 69.82% | +4.95pt (sign-consistent) |

목표(노이즈 5사이트 솎기 + 영구 필터)는 정량 달성. **활성 voc 4,678 archive·MX 절대수 보존(42,804 동일)·비율 자연 상승.**

## 2. F1 — worst 5 사이트 정리 (DRY → 본런)

DRY (04:25:53 UTC) → 본런 (04:26:20 UTC) one-shot, 트랜잭션당 1 사이트.

| code | 활성 (前) | mx_match | archive | mx_보존 | match% (前) |
|---|---|---|---|---|---|
| lowyat | 2,364 | 806 | **1,558** | 806 | 34.1 |
| computerbase | 1,708 | 581 | **1,127** | 581 | 34.0 |
| quasarzone | 1,304 | 427 | **877** | 427 | 32.7 |
| reddit_rss | 1,266 | 422 | **844** | 422 | 33.3 |
| donanimhaber | 423 | 151 | **272** | 151 | 35.7 |
| **5사이트 합** | **7,065** | **2,387** | **4,678** | **2,387** | 33.8 |

`COALESCE(content_translated, content_original) ~* '<unified pattern>'` 미매치 행에 `archived_at = NOW()` 일괄 적용. mx_match 행 100% 보존.

## 3. F2 — collector mx_keywords 영구 필터

5개 collector `crawl()` 의 최종 return 직전에 `from nlp.mx_keywords import is_mx_relevant; <list> = [v for v in <list> if is_mx_relevant(v.content)]` 패턴 삽입 (Data Clean 2 패턴 재사용):

- `crawler/platforms/donanimhaber.py` L168 (`return results`)
- `crawler/platforms/reddit_rss.py` L349 (`return all_vocs`)
- `crawler/platforms/computerbase.py` L147 (`return results`)
- `crawler/platforms/lowyat.py` L123 (`return raw_vocs`)
- `crawler/platforms/quasarzone.py` L115 (`return detailed`)

5개 모듈 import OK + `is_mx_relevant` smoke test (Samsung Galaxy True / random False). 다음 수집 사이클부터 신규 노이즈 진입 차단.

## 4. F3 — 라이브 + 다음 후보

**endpoint 검증**:
- `/api/v1/_internal/data-quality` → **200** (total/active 61,309 / mx_match_pct 74.4 / mx_rich_pct 50.5)
- `/api/v1/dashboard/overview?period=90d` → **200** (total_voc 61,309 / neg_rate 18.4 / top_product GS26U)
- frontend `:17370` → **200**, nginx `:8088` → 502 (Clean 2부터 pre-existing, 범위 외)

**글로벌 IT 무영향 확증**: hackernews 36,486 / anandtech 497 / notebookcheck 227 / xda 149 — Clean 4 직전 동일 (필터 영향 0).

**다음 worst 5 (Data Clean 5 후보, 모두 match% < 45)**:
- androidcentral 1,683 (34.6%)
- ruliweb 307 (36.5%)
- samsung_community 392 (38.0%) — 公式 커뮤니티지만 OS/액세서리 등 잡음 많음
- stackexchange 282 (39.7%)
- gsmarena_forum 251 (41.0%)

androidcentral은 절대 규모(1,683)로 Clean 5 1순위. samsung_community는 公式이라 archive 정책 사전 검토 필요.

## 5. OOM 안전성

swap 사용 **8,028MB → 8,028MB** (드리프트 0, ±500MB 허용 범위 내). free RAM 1,075MB → 1,428MB (+353MB). 메인 프로세스 PID 무변동 (uvicorn 941520 안정, 무중단). OOM kill 0건.

## 6. 잔여 / 후속

- **drift A** — `data-quality` endpoint가 archived=0/archived_pct=0.0 노출 (pre-existing 카운팅 버그, DB는 91,750 archive 정확). 페이지 표시값이 사용자에게 잘못된 진척 인상을 줄 수 있음. Clean 5에서 endpoint 패치 필요.
- **drift B** — endpoint mx_match (45,635) vs DB literal pattern (42,804) 약 2.8k 격차. 키워드 정규식 분기 (one_ui / aireuropa 등 별칭). 한 쪽으로 정규화 필요.
- **drift C** — collector 영구 필터는 다음 수집 cycle 발화 시점부터 검증 가능 (현 윈도우 미발화). 다음 라운드에 log tail 확인.
- **Clean 5** — androidcentral 우선 (절대 archive 잠재 ≥ 1,100). samsung_community는 공식 정책 결정 후 진행.
- **nginx 502** — Clean 2부터 누적, 별도 트랙 필요.
- **알렘빅** head 0019 무변동. MV 미관여 (worst5는 MV 슬라이스 외).

audit: `/home/koopark/claude/SignalForge/audit/data_clean4.jsonl` 5 entries (dry / archive / patch / live / drift). round=data_clean4 일관.
