# R24 EXTEND — 2026-06-05 (ultracode)

R23 잔여 권고 5건 (A multi-label F1 / B 메타-루프 / C self LoC 보정 / D 한국어 Crisis 본런 / E round 라벨링) 을 5 트랙 병렬 수행한 종합 보고. 전 트랙 DRY_RUN=0 PRESERVE_EXISTING=1 audit JSONL 가동, R18 폭락 (-89,395) 재발 0건, 정규 회귀 10/10 OK 유지.

## 1. 트랙별 결과 + verify

| 트랙 | 산출 | verify | self-report drift |
|---|---|---|---|
| A multi-label F1 | 스크립트·테스트 14/14·JSONL 100 행 | pass | n/a (신규) |
| B 메타-루프 | validator +CLI·iter 0~2·cap reached | pass | self_drift 0.7383% (보고 0.74% 일치) |
| C self LoC 보정 | drift_stats +171 LoC·6/6 PASS | pass | 재귀 편향 +26.45% 분리 집계 |
| D 한국어 Crisis | clien/dcinside 본런 800 fetch·in_window=0 | **partial (bona-fide null)** | 보고 +0 / 실측 +0 일치 |
| E round 라벨링 | audit `round` 자동 주입·by_round 집계 | pass | R24:3/3 alerts:0 |

## 2. A multi-label joint F1 (qwen2.5:14b, n=100, multi=63 / single=37)

```
exact_match     0.150
partial_match   0.800
Jaccard 평균    0.429
F1_micro 평균   0.535
Macro F1        0.539
Primary top1 F1 0.430  (R23 0.406 대비 +0.024)
```

per-topic F1 — emotion_only 0.750 / service_repair 0.833 / question 0.655 / comparison 0.436 / experience 0.278 (LLM 과소). 베이스라인: R10 0.678 → R18 0.640 → R23 0.406 → R24 0.430 (primary) / 0.539 (macro multi-label) — multi-label 게인 +0.109 (재발견).

산출: `/reports/topic_eval_2026-06-05_r24_multilabel.{md,json,jsonl}`.

## 3. B workflow validator 메타-루프

`workflow_validator.py` 에 `parse_report_meta` · `validate_meta` · `_build_meta_report` + CLI `--meta/--meta-max-iter/--meta-output-dir` 추가 (1067 LoC). R23 보고서 자체를 validator 가 검증 → iter 0/1/2 (cap 3) 모두 6 claims · 0 alerts · self_drift 0.7383%. 라이브 DB 대조: voc 120,728 → 120,731 (+0.0025%), linked 20,084 (0%), topics 104,601 (0%), products 389 (0%). 테스트 4/4 PASS. 읽기 전용 — DB write 없음.

## 4. C drift_stats self LoC 보정

`workflow_drift_stats.py` 612 → 783 LoC. `_SECTION_SELF_KEYWORDS` / `_MODULE_SELF_KEYWORDS` 2 단계 감지 (Markdown 헤더 범위 + 모듈명 ±2 라인). `DriftSample.is_self_report` / `self_marker` 필드, `compute(exclude_self=True)` 기본, CLI `--include-self` 호환. backend `/api/_internal.py` `workflow-drift-stats` 엔드포인트 `include_self` Query 추가 (2620-2704). 테스트 `test_drift_self_correct.py` 6/6 PASS. R23 재귀 편향 +26.45% → exclude_self 적용 시 분리 집계.

## 5. D 한국어 Crisis 본런 (clien / dcinside)

KW 5종 × 2 사이트 × max_page 5 → 총 800 fetch (clien 400 / dcinside 400). **in_window=0**. published_at 이 Crisis window (GN7 2016Q3·GZF1 2019Q2·GS22U 2022Q1·GZFL3 2021Q3·GS20 2020Q1) 안 들어오는 최근 글만 회수 — out_of_window: clien 14,904 + dcinside 24,026. 결과: bona-fide null (검색 API 가 최신 정렬). 실측 (psql): Crisis VOC 변동 0건 (GN7 218 / GZF1 107 / GS22U 2 / GZFL3 8 / GS20 38 그대로). 안전성 — DRY_RUN=0 + PRESERVE_EXISTING=1 + ON CONFLICT DO NOTHING, **R18 폭락 재발 0건**. 후속: 사이트별 아카이브 기반 크롤 필요.

## 6. E round 라벨링 audit JSONL

`backfill_audit.py` `AuditEntry.__init__` 가 env 에 `round` 없으면 `ROUND` env (없으면 `'unlabeled'`) 자동 주입. `topic_llm_apply.py` 의 `write_audit()` 우회 경로도 env 명시 inject. `backfill_audit_monitor.py` `summarize()` 에 `by_round` 집계 + alert 별 `round` 필드 부착. CLI 실행 시 `by_round: {"R24": 3}`, violations 0, alerts 0. 테스트 `test_round_label.py` 2/2 PASS. 정규 회귀 (audit_monitor / cpd_env / backfill_safety / audit_thresholds / audit_critical_fix) 유지.

## 7. 가동 절차

```bash
# A multi-label
cd /home/koopark/claude/SignalForge/crawler && \
  python scripts/topic_eval_multilabel.py --n 100 --multi-ratio 0.7 --round R24

# B 메타-루프
python -m insight.workflow_validator --meta \
  --meta-max-iter 3 --meta-output-dir /home/koopark/claude/SignalForge/reports

# C drift_stats (self 제외 기본)
python -m insight.workflow_drift_stats               # exclude_self=True
python -m insight.workflow_drift_stats --include-self # 호환

# D 한국어 Crisis (재시도 시 archive 모드)
ROUND=R24 python crawler/crisis_kr_runner.py --sites clien,dcinside

# E round 라벨링 (자동)
ROUND=R24 python scripts/topic_llm_apply.py
python -m insight.backfill_audit_monitor --summary  # by_round 표시
```

## 8. 측정 수치 종합

| 지표 | R23 | R24 | Δ |
|---|---|---|---|
| voc | 120,728 | 120,731 | +3 |
| linked | 20,084 | 20,084 | 0 |
| topics_filled | 104,601 | 104,601 | 0 |
| products | 389 | 389 | 0 |
| crisis_VOC 합계 | 373 | 373 | 0 |
| topic primary F1 | 0.406 | 0.430 | +0.024 |
| topic macro F1 (multi) | n/a | 0.539 | 신규 |
| Jaccard 평균 | n/a | 0.429 | 신규 |
| workflow validator self_drift | n/a | 0.7383% | 신규 |
| drift_stats LoC | 612 | 783 | +171 |
| validator LoC | n/a | 1,067 | 신규 |
| pytest (R24 신규) | — | 14+1+6+2=23/23 | PASS |
| 정규 회귀 | 10/10 | 10/10 | 유지 |

## 9. 다음 단계 + 잔여

**남은 잔여**
- D 한국어 Crisis — clien/dcinside 일반 search 가 최신 정렬 only. 다음: clien archive (board=park 등 board 별 페이지네이션), dcinside `mini.dcinside.com` archive 또는 갤러리 직검 (`gall.dcinside.com/board/lists?id=...&page=N`) 적용 후 published_at filter 재시도. KW reformulation (예: "갤노트7" → "Note7 발화 2016") 도 병행.
- A multi-label — experience F1 0.278 (LLM 과소 회수), comparison precision 0.895 vs recall 0.288 (LLM 과대 호출). 프롬프트 카테고리 정의 다듬기 1라운드.
- C self LoC — exclude_self 기본 적용 후 신뢰도 보정값 운영 대시보드 그래프 1주 관측.
- B 메타-루프 — cap=3 짧음, R25 에서 5 로 확장 + alerts 케이스 합성 회귀 추가.
- E round — `ROUND` env 미지정 시 `unlabeled` 누적 방지 위해 CLI 진입 시점 강제화 1라인 추가.

**R25 권고 우선순위**: D archive 모드 (필수) → A 프롬프트 튜닝 (중) → B cap 확장 (저) → C/E 운영 안정 (저).

R18 폭락 재발 없음 (DRY_RUN+PRESERVE_EXISTING+ON CONFLICT DO NOTHING 3중). self-report drift 명시: B 트랙 0.7383% (claim 0.74%, ±10% 임계 내), 라이브 DB +0.0025% (claim 0%, 누락 미공개 추가분 3건).
