# R9 활성화 보고 — 2026-06-04

R8 권고 5건 + α 를 5 트랙(A·B·C·D·E) 병렬로 종결. 각 트랙은 build/verify 페어를 거쳤고 partial 또는 mismatch 는 그대로 명시한다.

---

## 1. 트랙별 결과 + verify

| 트랙 | 주제 | build 핵심 | verify 결과 |
|---|---|---|---|
| **A** | 17년 lifecycle/위기 시각화 | backend 3 endpoint + deep_service `CRISIS_CATALOG` 3 사례 + frontend `/history` 페이지 + vitest | **PASS** — 빌드 크기 `History-Ce5_YExI.js 6.84 KB / gz 2.70 KB`, `index-CXnKoq_T.js 22.93 KB / gz 9.25 KB`, tsc clean |
| **B** | GOLD_ 옛 폰 정규식 보강 | `relink_products.py` 에 bare `Galaxy Mega` + 한국어 negative-lookahead 3 (`스타(?![터트])`, `미니(?!멀)`, `노트(?![북])`) + 테스트 8 신규 | **PASS (수치 partial)** — pytest 399/2skip, GMEGA63 +1 linked 확인. 단, build 보고의 "OLD 75→76" 은 실측 46-68 범위로 ±20% 상단 초과. 정성적 효과는 유효 |
| **C** | R6/R7/R8 회귀 자동 검증 | `/api/v1/_internal/regression-baseline` 9 메트릭 + threshold + alembic head + pytest 11 | **PASS** — 19/19 _internal green, 9/9 ok. 보고의 endpoint "74→75" 는 customer-facing 기준, 실측 총 등록 82(WS·helper 포함) |
| **D** | topic 분류기 정확도 spot-check | qwen2.5:14b 로 9 topic × 10 = 90 LLM 라벨, seed=20260604, 보고 MD+JSON | **PARTIAL** — 전체 accuracy **0.456 < 0.65 임계 미달**. price_purchase 모집단 수치 "11,639" 는 실측 7,428(±20% 초과). 평가 결과 자체는 재현됨 |
| **E** | Reddit/Bluesky 키 가이드 + Bluesky 슬롯 | REDDIT_OAUTH_GUIDE 1.5절 + BLUESKY_GUIDE 9절 + `.env` 슬롯 + `test_bluesky.py`/`test_reddit.py` runtime probe | **PASS** — vitest 실측 197/197(보고 190 보다 상회), 모든 파일 라인 수/섹션 일치 |

verify 의 partial/mismatch 는 D(정확도 미달 + 수치 1건), B(OLD 카운트 상단 초과) 두 건이며 사용자에게 그대로 보고된 상태.

---

## 2. 신규 endpoint 표 (A 3 + C 1 = 4)

| Method | Path | 그룹 | 응답 스키마 | 비고 |
|---|---|---|---|---|
| GET | `/api/v1/deep/galaxy-timeline` | deep | `GalaxyTimelineResponse` | series 별칭(S/Note/Z/A…) → 카탈로그 모델 리스트 |
| GET | `/api/v1/deep/crisis-cases` | deep | `CrisisCasesResponse` | Note7/Fold1/S22 GOS 3 사례 + VOC count |
| GET | `/api/v1/deep/series-comparison` | deep | `SeriesComparisonResponse` | S vs Note vs Z 누적 VOC·linked 비율 |
| GET | `/api/v1/_internal/regression-baseline` | _internal | inline 9 metric | localhost-only, alembic head 동시 검증 |

총 backend 라우트 카운트: customer-facing **74 → 75** (R8 → R9), 전체 등록(WS/helper 포함) **82**.

---

## 3. /history 페이지 동작

- 경로: `/home/koopark/claude/SignalForge/frontend/src/pages/History.tsx`
- 상단: series selector(S/Note/Z/A/M/J) → galaxy-timeline 응답 44 모델 시각화 (GS1 2010-06-04 → GS25U 2026-01-22)
- 중단: crisis-cases 3 카드 (Note7 발화 352 VOC, Fold1 힌지 308 VOC, S22 GOS 415 VOC) — 클릭 시 VOC 드릴다운 모달
- 하단: series-comparison 누적 VOC 라인차트 (S vs Note vs Z 누적·linked 비율)
- 코드분할: `History-Ce5_YExI.js` 6.84 KB / gz 2.70 KB, lazy-load main 22.93 KB / gz 9.25 KB
- vitest 197/197 green

---

## 4. GOLD 옛 폰 매칭 결과

- 정규식 보강: bare `\bgalaxy\s+mega\b` 1, 한국어 negative-lookahead 3 (스타·미니·노트)
- MAP substring 정제: 의도치 않은 5건 제거(스타터·미니멀·노트북 오탐)
- 라이브 효과: GMEGA63 +1 (보고 헤드라인 "6.3in Galaxy Mega" 매칭), 한국어 오탐 0
- OLD 시리즈 전체 linked 카운트: 46 ~ 68 (서브셋에 따라 변동) — 보고의 "75→76"은 상단 초과로 partial 표시
- pytest 8 신규 + 회귀 46 모두 green, crawler 399/2skip

---

## 5. 회귀 baseline 표 (`/api/v1/_internal/regression-baseline`, 2026-06-04 01:48 KST)

| name | current | baseline_R8 | threshold | delta | ok |
|---|---|---|---|---|---|
| note7_voc | 352 | 352 | 300 | 0 | ✓ |
| fold1_voc | 308 | 302 | 280 | +6 | ✓ |
| s22_voc | 415 | 412 | 380 | +3 | ✓ |
| hn_total | 33,533 | 33,528 | 33,000 | +5 | ✓ |
| products | 389 | 389 | 380 | 0 | ✓ |
| voc_records | 161,491 | 161,300 | 160,000 | +191 | ✓ |
| linked | 33,663 | 33,650 | 33,000 | +13 | ✓ |
| topic_classified | 42,935 | 42,900 | 42,000 | +35 | ✓ |
| alembic_head | 0013 | 0013 | == | 0 | ✓ |

summary `{total:9, ok:9, failed:0}` — 모든 R6/R7/R8 핵심 수치 유지.

---

## 6. topic 정확도 결과 (PARTIAL)

- 모델: `qwen2.5:14b`, seed=20260604, primary=topics[1]
- 표본: 9 topic × 10 = 90 (모집단 42,935 균등 sampling)
- **전체 accuracy: 0.456 — 임계 0.65 미달**

| topic | precision | F1 | 신뢰 |
|---|---|---|---|
| emotion_only | 1.000 | 0.952 | 압도적 |
| service_repair | 0.600 | — | 보통 |
| expectation | 0.600 | — | 보통 |
| question | 0.500 | — | 보통 |
| comparison | 0.500 | — | 보통 |
| experience | 0.400 | — | 약함 |
| negative_general | 0.300 | — | 약함 |
| positive_general | 0.100 | — | 매우 약함 |
| price_purchase | 0.100 | — | 매우 약함 |

판정: 자동 분류기는 `emotion_only` 외에는 LLM 라벨과 합의 낮음. 후속 작업 — 룰베이스 정제 + 14b 라벨링 일부 재학습 + price_purchase 모집단 재계산 (실측 7,428 기준).

---

## 7. Reddit/Bluesky 가이드

- `REDDIT_OAUTH_GUIDE.md` 1.5 절 신규 — 2024 Developer Platform 폼 항목 표 + "AI training: No" 답안 + Approved/pending 확인 절차 (107 라인)
- `BLUESKY_GUIDE.md` 신규 9 절 — 가입 → 앱패스워드 → `.env` → 활성 → 검증 → pytest → 트러블슈팅 → 스케줄 → 메트릭 (94 라인)
- `.env` 슬롯: `BLUESKY_HANDLE`, `BLUESKY_PASSWORD` + 가이드 경로 주석 (라인 58-63)
- runtime probe: `crawler/scripts/test_bluesky.py`, `test_reddit.py` — 키 무 → exit 0 + 가이드 출력, 키 유 → collected=N + sample 3건

---

## 8. 가동 절차

1. Reddit 키 신청: `docs/dashboard/REDDIT_OAUTH_GUIDE.md` 1.5 절 폼 표 그대로 입력 → Approved 확인
2. Bluesky 가입 → 앱패스워드 발급 → `.env` 의 `BLUESKY_HANDLE`/`BLUESKY_PASSWORD` 입력
3. probe 실행: `python crawler/scripts/test_bluesky.py` → collected ≥ 1 확인
4. 스케줄러 활성: `BLUESKY_GUIDE.md` 8절 cron 등록
5. 회귀 검증: `curl localhost:8000/api/v1/_internal/regression-baseline` → `{ok:9, failed:0}`
6. UI 확인: `/history` 페이지 series=S 로딩 → crisis 카드 3건 표시
7. topic 후속: D 트랙 partial 사유 reviewer 에게 공유 → R10 에서 룰 정제 + price_purchase 재라벨링

---

## 9. 측정 수치 종합

| 항목 | R8 | R9 |
|---|---|---|
| products | 389 | 389 |
| voc_records | 161,300 | 161,491 (+191) |
| linked | 33,650 | 33,663 (+13) |
| topic_classified | 42,900 | 42,935 (+35) |
| backend endpoint (customer) | 74 | 75 |
| backend endpoint (total reg.) | — | 82 |
| pytest | 49/50 | backend 21+11 신규 추가, crawler 399/2skip |
| vitest | 190/190 | **197/197** |
| frontend main | 22.66 KB | 22.93 KB / gz 9.25 KB |
| 신규 page | — | `/history` 6.84 KB / gz 2.70 KB |
| topic 정확도 | 미측정 | **0.456 (임계 미달)** |
| OLD 시리즈 linked | 75 | 46-68 (서브셋 변동, +1 GMEGA63) |
| alembic head | 0013 | 0013 |

---

## 10. 다음 단계 (R10 후보)

1. **topic 분류기 재학습** — D 트랙 partial 정면 대응. price_purchase·positive_general 룰베이스 강화 + 14b 라벨 1k 샘플 재학습
2. **OLD 시리즈 카운트 정밀화** — B 트랙 partial. linked 카운트의 서브셋 정의 문서화 + dashboard 표 분리
3. **Bluesky 실 활성 후 메트릭** — E 트랙 가이드대로 키 입력 후 24h 수집량 측정 → /api/v1/_internal/regression-baseline 에 bluesky_daily 추가
4. **회귀 임계 자동 튜닝** — C 트랙 baseline 을 7일 이동평균으로 자동 갱신 (현재 수동 hard-code)
5. **/history 인터랙션** — crisis 카드 → VOC 드릴다운 모달의 LLM 요약 1줄 추가 (캐시 4700× 활용)
6. **Reddit OAuth Approved 후** — collector 활성 + r/Galaxy r/Android voc 수집 재개

verify 결과 D·B 두 트랙의 partial 은 명시 유지. 나머지 A·C·E 는 build 와 verify 가 일치.
