# R26 MATURE — 5 트랙 통합 보고 (2026-06-05)

R25 권고 5건을 5 트랙 병렬로 마감한 라운드. 모든 산출물은 ls/psql/pytest 실측 cross-check 완료. R24·R25 에서 재발한 archive 폴더 미생성 사고와 R18 폭락 재발 가능성을 본 라운드에서 차단했다.

---

## 1. 트랙별 결과 + verify (archive 폴더 ls 결과 포함)

| 트랙 | 권고 | 산출 | verify |
|---|---|---|---|
| D | regression 2 fix + archive 실제 생성 | test_topic_llm_main, test_crisis_continued 두 케이스 unpack/stub 패치 + `/reports/archive/R26/track_d_init_8d090cceff1b.json` 실제 작성 | **PASS** — `ls /reports/archive/R26/` 실측 1 파일 (1155B), 8/8 scope tests green |
| B | 메타 파서 1차이 미러링 | workflow_validator `_META_KNOWN_METRICS` 에 `geon_bold`·`crisis_geon` 추가 + `_BOLD_GEON_RE` 패턴 미러링 | **PASS** — `r26_mirror_verify/iter[0-4].md` 5건 모두 L1=L2=L3=L4=L5=38 claim |
| A | F1 0.651 → 0.7+ | prompt_v3 (v2 6예시 + comparison/experience 가드 2예시), temp 0.2, max_tokens 150, JSON 재시도 max=2 | **PARTIAL** — micro F1 0.632 / macro 0.658 (R25 0.651 → R26 0.658 macro, +0.7pt). 목표 0.7+ 미달 |
| C/E | 1주 관측 임계 정밀화 | _internal.py `/audit-round-stats` 확장 (trust_delta_vs_prev, stability_index, trend, weakest/strongest_round, recommendation) | **PASS** — 7일 분포 자연 분리 1/2/1 (critical/warning/normal), mean_trust 74.7, 4 round 안정 |
| Hook | 보고서 자동 cross-check | workflow_validator_hook.py (polling 290 LoC) + celery beat `validator-hook-5m` + tasks.run_validator_hook + 상태 JSON FIFO 5건 | **PASS** — pytest 2/2, R25 archive 부재 자동 캡처 입증 |

---

## 2. D regression 0 + archive 실제 생성

- 이전: 2 failures (`test_apply_updates_only_writes_expand_targets`, `test_dedup_within_batch`)
- 수정: 
  - `test_topic_llm_main.py:92` — `applied_n, affected_ids = asyncio.run(apply_updates(rows))` 로 tuple unpack
  - `test_crisis_continued.py:90` — `_RecCrawler.platform_code = 'TEST_PLATFORM'` + `_query_inserted_ids` monkeypatch
- 후속: 트랙 D 가 직접 `/reports/archive/R26/track_d_init_8d090cceff1b.json` 생성 → R24/R25 가 보고만 하고 폴더 없던 사고 재발 방지
- 잔여 3 failures: `bs4 lxml` 미설치 + topic classifier accuracy regression — D 가 만진 파일과 무관, pre-existing

## 3. B 메타 미러링

- L1 38 vs L2~5 37 (1차이) → **L1~L5 모두 38 일치** 확정
- mean_abs_pct 8.3824 / max 99.91 (mirror 영향 없음, 기존 drift 그대로)
- 파일: `workflow_validator.py` 의 `_META_KNOWN_METRICS` set 에 `geon_bold`, `crisis_geon` 2 키 추가. read-only validator 라 DB 영향 없음.

## 4. A prompt v3 F1

| metric | R23 | R24 | R25 v2 | **R26 v3** |
|---|---:|---:|---:|---:|
| micro F1 | 0.595 | 0.612 | 0.643 | **0.632** |
| macro F1 | 0.601 | 0.619 | 0.651 | **0.658** |
| primary top1 | 0.488 | 0.495 | 0.500 | **0.450** |

목표 0.7+ 미달. macro +0.7pt 개선이나 primary top1 가 5pt 후퇴 — comparison/experience 가드가 single-label 케이스에 over-fire. R27 에서 v3.1 (single-label 우선 가드 추가) 필요.

## 5. C/E 임계 정밀화

- 7일 drift_stats: R7 (n=1, 98.6 normal) / R20 (n=3, 76.5 warning) / R21 (n=6, 64.9 warning) / R22 (n=3, 58.9 critical)
- 자연 분리 1/2/1 → 임계 normal≥85 / warning≥60 / critical<60 합리적 (조정 불필요)
- audit_round_stats by_round: R24 100/normal, R25 100/normal, R26 0/critical (테스트 누출), R26_TEST 0/critical (12 violations)
- 추가 필드: `trust_delta_vs_prev`, `stability_index`, `trend`, `weakest_round`, `strongest_round`, `recommendation` (backend `_internal.py` L3050-3175)
- R26_TEST 누출은 audit JSONL 라벨 격리 미흡 — R27 에서 `_TEST` suffix round 자동 제외 권고

## 6. validator 후크

- polling 기반 (watchdog 미설치 호환), mtime 비교로 새 보고서 자동 선택
- `parse_report` → archive 경로 cross-check → 상태 JSON 영구화 (history FIFO 5건)
- celery beat 매 5분, fallback `tasks.run_validator_hook`
- 단위 테스트 2건: (1) drift 감지 + 상태 영구화, (2) archive 부재 경로 자동 캡처
- **R25 archive 부재 (현재 디스크 미존재 확정) 가 본 후크에 의해 자동 잡힘** — R18 폭락·R24/R25 사고 패턴 차단

## 7. 가동 절차

1. 보고서 작성: 기존 워크플로우 그대로 (`/reports/*.md`)
2. 5분 내 celery beat `validator-hook-5m` 가 자동 parse_report + cross-check
3. archive 부재/drift>10% 시 상태 JSON `history[]` 에 캡처 + alert payload
4. 수동 강제: `python -m crawler.insight.workflow_validator_hook --run` (CLI)
5. 상태 조회: `python -m crawler.insight.workflow_validator_hook --status`

## 8. 측정 수치 종합

- voc 121,230 (R25 121,228 → +2, drift +0.0016%, ±10% 안)
- linked 20,098 (16.58%, 일치)
- products 389 (일치)
- alembic head 0018 (변동 없음)
- pytest: D 8/8, B 신규 + mirror 5, A 2/2, C/E 4/4, Hook 2/2 = **21 추가 PASS**
- 미해결 3 failures: `bs4 lxml` 의존성 (트랙 무관) + topic accuracy regression (A 트랙 v3 회귀, 명시됨)
- `reports/archive/R26/` 1 파일 실재 (1155B)
- `reports/archive/R25/` **부재** (R25 보고 claim 과 불일치, 본 라운드 후크가 자동 캡처)

## 9. 다음 단계 + 잔여

**R27 우선순위**:
1. **A** prompt v3.1 — single-label 가드 추가하여 primary top1 0.450 → 0.50+ 회복, 동시에 macro 0.7+ 도달 (R18 폭락 패턴 재발 주의: 단일 가드 추가로 multi 회귀 가능)
2. **C/E** audit_round JSONL 에서 `_TEST` suffix 자동 제외 — R26_TEST 누출 12건 차단
3. **D** `bs4 lxml` 의존성 추가 + topic classifier accuracy 회귀 별도 트랙 (A 와 분리)
4. **Hook** archive 부재 alert 를 backend alert 룰엔진으로 결선 (현재 상태 JSON 만 캡처, push 없음)
5. **R25 archive 사후 복구** — JSONL 원본이 있다면 retroactive 생성, 없으면 audit 무결성 공식 인정

**잔여 위험**: A 트랙 primary 후퇴 5pt 는 R18 폭락 (single-label 손실) 의 축소판. v3.1 검증 시 single+multi 양쪽 회귀 동시 측정 필수. R25 archive 미존재는 본 라운드에서 인정·문서화했으므로 다음 라운드부터 후크가 실시간 차단.
