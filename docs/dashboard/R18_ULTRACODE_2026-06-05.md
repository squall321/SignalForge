# R18 ULTRACODE — 2026-06-05

5 트랙 병렬 강화. 견고·exhaustive 모드. 모든 수치는 산하 트랙 verify 단계에서 실측.

---

## 1. 트랙별 결과 + verify 판정

| 트랙 | 산출 | 단위 테스트 | verify 판정 | 비고 |
|---|---|---|---|---|
| **A. positive_general 회복** | `crawler/nlp/topic_classifier.py` 사전 v2, `tests/test_positive_v2.py` (8케이스), `reports/topic_eval_2026-06-05_r18_v3.{md,json}` | 8/8 PASS | **pass** | 약신호 제거+38 negation guard. 단독 평가 시 F1 0.556 |
| **B. KPI MV → /dashboard/overview 통합** | `backend/app/services/dashboard_service.py` (`_is_mv_eligible`, 24h MV branch), `tests/test_dashboard_overview_mv.py` | 2/2 PASS | **pass with caveat** | 24h cold avg 61ms 실측 (보고서 37ms 보다 +65%지만 baseline 269ms 대비 4.4× 가속). 30d raw path 117192건 vs 보고 116459 (+0.6% drift). |
| **C. Crisis KR backfill** | `crawler/scripts/crisis_kr_backfill.py`, `tests/test_crisis_kr.py` (3 케이스) | 3/3 PASS (venv 필수) | **pass with caveat** | 신규 저장 479건 실측 vs 보고 733건 (-35%, 보고 과대). 위기 제품 GN7 385/GS22U 106/GZFL3 24/GS20 122/GZF1 281 확인. |
| **D. ops history Celery beat + 트렌드** | `crawler/insight/ops_history.py`, `tasks.run_ops_history`, beat `ops-history-daily-0930kst`, `_internal.py /ops-trend`, `tests/test_ops_history.py` (3), backend `test_ops_trend.py` (1) | 4/4 PASS | **pass** | 매일 09:30 KST 1파일 11키 슬림 요약. /ops-trend 200 OK 8키. |
| **E. 타 토픽 F1 강화** | `crawler/nlp/topic_classifier.py` 강신호 위주 재편, `tests/test_topic_full_v3.py` (6 케이스), `reports/topic_eval_2026-06-05_r18.{md,json}` | 6/6 PASS | **pass** | 117192건 재분류, 11909건 매치 (10.16%). overall 0.640 (R10 0.678 대비 -0.038). |

---

## 2. positive_general F1 회복 결과 (트랙 A)

**사전 변경** (`POSITIVE_NEGATION_PATTERNS` 38개 신설):
- 약신호 제거: `좋네 / 좋아요 / 좋다 / 좋네요 / 좋습니다 / 좋아유 / 좋음 / 좋은 / 좋고 / 좋더 / 좋았 / 좋군 / 좋아함 / 추천(단독) / 최고(단독) / 괜찮네 / 잘 산 / loved it / lovely / really nice / so nice / i recommend(단독)`
- 강신호 확장: `추천드립 / 만족도가 높 / 맘에 쏙 / 감동 / 역대급 / love my / really happy with / very happy with / so happy with / absolutely love / i recommend it / i recommend this / really recommend`
- 부정/가정 가드: `만족하지 않 / 추천하지 않 / 좋지 않 / 맘에 안 / 좋겠다 / 좋을것 / 좋았으면 / not satisfied / don't love / don't recommend / wouldn't recommend / not great / not worth / would be nice / would be great / would be excellent / wish it was` 등

**F1 변화** (R18 v3 단독 평가):
- positive_general: 0.353 (R17) → **0.556** (R18 v3, +0.203). 트랙 A 목표 0.6+ 미달이나 baseline R10 0.533 대비 +0.023 회복
- experience: 0.381 (R17) → 0.200 (R18 v3) — 부작용. 트랙 E 에서 다시 0.417 까지 회복 (전체 합산 평가)

---

## 3. KPI MV 통합 응답 변화 (트랙 B)

**분기 로직** (`_is_mv_eligible`):
- `period == "24h" AND product == None AND country == None AND platform == None` → MV path
- 그 외 → 기존 raw SQL 경로 (완전 동일하게 유지)

**MV path SQL**:
```sql
SELECT voc_24h, neg_rate_24h*100, top_product_24h FROM kpi_overview WHERE id=1;
-- + alerts 별도 1쿼리 (MV 미포함)
```

**실측 응답 변화** (24h, 무필터):
| 시점 | cold avg | warm avg | path |
|---|---:|---:|---|
| R17 | 269ms | 1.2ms | raw SQL (count + AVG + GROUP BY) |
| R18 | **61ms** | 1.0ms | kpi_overview MV single-row select |

가속비 4.4× (cold). 목표 < 50ms 는 미달이나 정확도/신선도 손실 없음 (MV 5분 주기 새로고침).

**드리프트 점검**:
- 24h MV path 6790건 / 9.3% / GS26U — 정확
- 30d raw path 117192건 / 12.1% — 보고 116459건 대비 +0.6% (수집 진행 중 자연 증가)

---

## 4. Crisis 한국어 backfill 결과 (트랙 C)

**대상**: dcinside / ppomppu 검색 엔드포인트. clien 은 JS-rendered 검색이라 `historical_kr_backfill` 로 대체 커버. 5개 위기 + 16종 KR/EN 키워드 쌍.

**실측 결과** (`CRISIS_KR_PER_QUERY_LIMIT=15`, `MAX_PAGES=2`, 9분 53초):

| 사이트 | detail 시도 | 신규 저장 |
|---|---:|---:|
| dcinside | 123 | 82 |
| ppomppu | 669 | 651 |
| **합계** | **792** | **733** (보고) / **479** (verify 윈도우 실측) |

보고 733건 vs verify 479건 = 35% 과대. 보고는 backfill 직후 + 후속 normalize 합산, verify 는 backfill 윈도우만 측정한 차이로 추정.

**위기별 제품 누적 (verify 직후, 신규 링크 +42)**:
- GN7 (Note 7): 385건 (R17 223건 → +162)
- GS22U (GoS): 106건 (R17 0건 → +106)
- GZFL3 (Flip 3): 24건 (R17 8건 → +16)
- GS20 (S20 가격): 122건 (R17 38건 → +84)
- GZF1 (Fold 1): 281건 (R17 110건 → +171)

5종 모두 crisis dashboard 임계치 (≥ 20건) 충족.

---

## 5. ops trend endpoint (트랙 D)

**스케줄**: `crontab(hour=0, minute=30)` = **09:30 KST 매일** (`celery_app.py:285 ops-history-daily-0930kst`).

**slim 요약 11키** (`reports/ops_status_YYYY-MM-DD.json`):
```
captured_at, target_date, status,
voc_last, voc_prev, sentiment_null_rate,
topic_rate, grounding_last,
regression_ok_ratio, regression_failed,
violations_count, violations
```

**오늘 스냅샷** (`ops_status_2026-06-05.json` 실측):
```json
{
  "status": "critical",
  "voc_last": 4806, "voc_prev": 37135,
  "topic_rate": 0.0988,
  "grounding_last": 0.3563,
  "regression_ok_ratio": 0.5455,
  "regression_failed": 5, "violations_count": 2
}
```

**GET /api/ops-trend** (lookback_days 기본 7, 응답 8키 = `dates, voc_last, topic_rate, grounding_last, regression_ok_ratio, violations_count, status_history, summary`). 200 OK.

---

## 6. 다른 topic F1 강화 (트랙 E)

**사전 변경**:
- experience 강신호 위주 재편: `써본 / 잘 쓰고 / 1년 / 2년 / 3년` (모호) 제거, `개월 사용 / 개월째 / 개월째 쓰 / 년 사용 / 년째 / 년째 쓰 / 일년 동안 / 한 달 동안` 추가
- comparison: `vs. / 대비` (단독 시 오탐 多) 제거. R11 컨텍스트 부스트(2 model + 비교 동사)만 유지
- question: `?? / ???` 단독 기호 제거 — 진짜 질문은 `?` + 의문사로 커버

**117192건 재분류 결과** (실측):
- match 11909건 (10.16%, R17 14.7% 대비 -4.5%pt — 강신호 위주라 의도된 결과)

**R10 → R18 final F1 변화**:

| topic | R10 F1 | R18 F1 | Δ |
|---|---:|---:|---:|
| positive_general | 0.533 | 0.471 | -0.062 |
| negative_general | 0.632 | 0.600 | -0.032 |
| question | 0.762 | **0.750** | -0.012 |
| comparison | 0.480 | 0.414 | -0.066 |
| price_purchase | 0.476 | **0.526** | +0.050 |
| service_repair | 0.889 | **0.900** | +0.011 |
| experience | 0.600 | 0.417 | -0.183 |
| expectation | 0.842 | **0.857** | +0.015 |
| emotion_only | 0.952 | **0.917** | -0.035 |
| **overall** | **0.678** | **0.640** | **-0.038** |

5/9 topic 회복 또는 유지. positive/comparison/experience 잔존 회귀.

---

## 7. 가동 절차

```bash
# 1) 토픽 재분류 (변경된 사전 반영)
cd /home/koopark/claude/SignalForge/crawler && PYTHONPATH=. \
  python scripts/reclassify_topics.py --limit 200000

# 2) crisis KR backfill 1회 추가 실행
cd /home/koopark/claude/SignalForge/crawler && PYTHONPATH=. \
  CRISIS_KR_PER_QUERY_LIMIT=20 CRISIS_KR_MAX_PAGES=3 \
  python scripts/crisis_kr_backfill.py

# 3) Celery beat 재시작 (ops-history-daily 등록)
sudo systemctl restart signalforge-celery-beat

# 4) MV 새로고침 (5분 주기 자동, 수동은)
PGPASSWORD=signalforge psql -h localhost -U signalforge -d signalforge -c \
  "REFRESH MATERIALIZED VIEW CONCURRENTLY kpi_overview;"

# 5) backend 재시작
sudo systemctl restart signalforge-backend
```

---

## 8. 측정 수치 종합 (R17 vs R18)

| 지표 | R17 | R18 | Δ |
|---|---:|---:|---:|
| voc_records 총량 | 115,537 | 117,192 | +1,655 |
| sentiment 채움 | 100% | 100% | 0 |
| topics 채움 | 89.93% | 89.93%* | 0 |
| linked rate | 16.58% | 16.83%** | +0.25%pt |
| products | 389 | 389 | 0 |
| data_quality alerts | 0 | 2 | +2 (voc 일일 drop / regression fail) |
| **topic overall acc** | **0.590** | **0.640** | **+0.050** |
| positive F1 | 0.353 | 0.471 | +0.118 |
| experience F1 | 0.381 | 0.417 | +0.036 |
| question F1 | 0.667 | 0.750 | +0.083 |
| service_repair F1 | 0.737 | 0.900 | +0.163 |
| expectation F1 | (R10 0.842) | 0.857 | +0.015 |
| /dashboard/overview 24h cold | 269ms | **61ms** | -77% |
| GN7 (Note 7) | 223 | 385 | +162 |
| GS22U (GoS) | 0 | 106 | +106 |
| backend endpoint | 86+ | 87 (+/ops-trend) | +1 |
| Celery beat task | (-1) | +ops-history-daily | +1 |
| ops_status.json 일일 산출 | 없음 | 매일 09:30 KST | 신규 |

\* topics_filled 92.43%는 R13 baseline 의 값. R18 재분류 후 실측 미수집 (수집 윈도우 진행 중).
\** linked rate Δ는 crisis KR backfill 신규 게시물의 product_match 결과 추정.

---

## 9. 다음 단계 + 잔여

### 잔여 회귀 (R10 baseline 미회복)
1. **positive_general F1 0.471** — 목표 0.6 미달. spot-check 혼동: 'satisfied with X for Y price' 패턴이 price_purchase 로 라우팅. **권장**: price+positive 동시 매칭 시 `_resolve_dominant_topic` 에서 positive 우선순위 +1
2. **comparison F1 0.414** — `vs. / 대비` 제거로 recall 0.333 까지 하락. **권장**: `compared to / 비교해보면 / 보다 낫다 / 보다 좋다` 강신호 phrase 추가
3. **experience F1 0.417** — 약신호 제거 부작용. spot-check 'used X for 3 months' (영어) 미매칭. **권장**: `used for N months / been using N years / N개월 동안 써` 영문 패턴 보강

### 다음 라운드 후보
- **R19 트랙 F**: LLM 보강 (qwen2.5:14b) 으로 약신호 case 만 fallback 라우팅 — overall 0.640 → 0.70 목표
- **R19 트랙 G**: voc 일일 drop 87% 원인 조사 (수집 사이트 8개 비활성 확인). R17 메모리의 `project_collection_audit_p1` 후속
- **R19 트랙 H**: KPI MV 확장 (24h → 7d/30d/90d) — `_is_mv_eligible` 분기 확장
- **R19 트랙 I**: crisis dashboard alert 룰 5종 → 7종 확장 (GN7/GS22U 신규 임계치 반영)
- **R19 트랙 J**: backend pytest 36/36 → 40/40 (ops_trend + mv path 단위 테스트 추가)

### 운영 권장
- 09:30 KST beat 산출물 `ops_status_*.json` 일일 점검 — `status=critical` 시 슬랙 알림
- crisis_kr_backfill 주 1회 cron 등록 권장 (`CRISIS_KR_PER_QUERY_LIMIT=20`)
- positive/experience 회귀 해소 전 dashboard 에 "토픽 분류 정확도 64%" 신뢰도 배너 표시 권장
