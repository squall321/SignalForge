# DATA_GROW_R3 — 2026-06-09

OOM cautious gate(swap free 125MB)에서 J1~J6 6트랙을 직렬 수행. 신규 사이트 4종(HackerOne·4chan /g/·Misskey·Lemmy 확장) + arXiv 카테고리 확장 + worker 정식 재기동(J4 동시 처리). 라이브 무중단(backend 18000 / frontend 17370 / postgres 5434 200) 유지, nginx 8088 만 502 잔존(드리프트).

## 1. 종합 Before/After
- voc_records 총: **155,237 → 155,693** (+456, 5트랙 실순증)
- 플랫폼별 신규: lemmy 508 / misskey 233 / fourchan_g 113 / arxiv 49→66(+17) / hackerone 0→9
- 24h voc: **3,571 → 3,809**
- product_id 매핑율: 15.70% (R3 컨텍스트 "mx_match% 76.2%"는 별도 MV/지표로 추정 — **드리프트 명시**)
- swap free: **125MB → 132MB** (+7, J4 worker 재기동으로 누적 RSS 해제, mem_avail +1.8GB 회복)
- worker: PID 58607 → **3702842** ( -c 6, J4에서 정식 SIGTERM 재기동, CRAWLER_MAP fourchan_g/hackerone/misskey 등록)
- alembic head: 0019 유지

## 2. J1 OOM 게이트
- 결정: **cautious** (swap free 125MB < 300MB, R2 124MB 동일 위험권)
- 정책: 1–2 트랙 직렬, 트랙 간 free -m 재측정 필수, -c 6 고정, -c 8 금지
- audit start만 작성(gate-only 의도) — end 미작성 드리프트 인정
- backend `/api/v1/health` 404 — 라우트 경로 컨텍스트 불일치 발견(`/docs` 200으로 프로세스 alive 확인)
- verdict: **pass** (게이트 결정 자체 정합, swap free 자연 회복)

## 3. J2 Lemmy 추가 instance
- 변경: `lemmy.py:L23` INSTANCES 2 → 4 (+sh.itjust.works, +lemmy.ml)
- DB: lemmy 495 → **508** (+13, 4h beat tick 1회 실측, audit `db_voc_delta:13` 일치)
- ap_id seen_keys로 인스턴스간 dedup 정상
- swap free: 125 → 132MB
- audit 1라인(start+end 융합) — 정책상 2라인 권고 partial
- verdict: **pass**

## 4. J3 HackerOne 신규
- 신규 파일: `hackerone.py` (실측 163L, 보고 165L drift -2) + 8 test pass(0.03s)
- platform id=103 / region=GLOBAL / 12h beat (43200s)
- voc: 0 → **9** (samsung 2/7, pixel 7/15, galaxy/android 0 — MX 필터 컷)
- 익명 API queryString 4종, MIN/MAX_DELAY 4–6s 보수
- 워커 미반영 → standalone async persist (J4에서 정식 재기동되어 다음 12h tick부터 beat 라인업 진입)
- disclosed_at 2013–2023 분포 — 다음 라운드 sort desc 검토 필요
- verdict: **pass**

## 5. J4 4chan /g/ Mobile thread
- 신규 파일: `fourchan_g.py` (실측 151L, 보고 132L drift +19) + 3 pytest pass(0.03s)
- platform id=105 / region=global / 2h beat (7200s)
- voc: 0 → **113** (catalog 12 matched, dedup_drop 1, MX 2차 컷)
- RELEVANCE_RE 단어경계 + HTML sanitize(quote/br/wbr)
- **worker 정식 재기동 수행** (TERM PID 58607 → 신 PID 3702842 -c 6, /tmp/sf_logs/worker_r3_j4.log)
- 재기동 결과 fourchan_g/hackerone/misskey 동시 라이브 등록 → mem_avail +1.8GB 회복
- verdict: **pass** (정상 celery task 파이프라인 persist)

## 6. J5 arXiv 카테고리 확장
- 변경: `arxiv.py:L27` SEARCH_QUERY +cs.NI, +eess.SP (2라인)
- voc: 49 → **66** (+17, MX 필터 17/50 = 34%)
- 샘플: SensorLLM HAR / Traffic-Pattern / PPG BAN / EEG diffusion / Wearable ECG
- L36 docstring 잔재 카테고리 미갱신 (cosmetic, 기능 영향 없음)
- standalone 실행 (worker 미반영, J4 재기동 직전 시점)
- verdict: **pass**

## 7. J6 Misskey 신규 (fediverse 확장)
- 신규 파일: `misskey.py` (8777B) + `test_misskey.py` 5 pass(0.07s)
- platform id=104 / region=JP / base misskey.io / 4h beat (14400s)
- 3 인스턴스(misskey.io/design/systems) × 7 쿼리, fan-out 21
- live save run: 2 inst × 5 q 보수 → fetched 257, MX pass 240(93.4%), saved **233**, dedup loss 7
- 언어: ja 위주(163/233) — 일본 Galaxy 커뮤니티 흡수 검증
- external_id md5 16자(인스턴스간 dedup)
- audit done 라인만 1건 (start 누락 partial)
- verdict: **pass**

## 8. 라이브 + 잔여
**라이브 무중단**: backend:18000=200 / frontend:17370=200 / postgres:5434 alive / worker PID 3702842 -c 6 alive (RSS 정상). swap free 132MB, mem_avail 58.5GB.

**잔여 드리프트**:
1. **nginx:8088 → 502 (`/`/`/health`/`/api/health` 전부 fail)** — J3/J4 verify에서 식별. nginx daemon listen 자체는 LISTEN 유지하나 upstream proxy 경로 fail. R3 작업과 인과 미증명(작업 이전부터 상태일 가능성), 다음 라운드 H6 우선 복구 필요.
2. R3 컨텍스트 mx_match% 76.2% 는 실측 product_id 매핑(15.70%)과 5배 격차 — 별도 MV/지표 추정, 재정의 필요.
3. R2 잔여 후보 7개 중 5개 완료(Lemmy/HackerOne/4chan/Misskey/arXiv). **미진행 2개**: Quora(rate limit 위험), Pikabu(러시아 게시판). 다음 라운드 검토.
4. arxiv.py L36 docstring 카테고리 갱신 잔여 (cosmetic).
5. J2/J6 audit 2라인 권고 부분 미준수 — 운영 규칙 갱신 검토.

**다음 우선순위**: nginx 502 원인 추적 → Pikabu/Quora 후보 평가 → mx_match% 정의 통합 → beat 정기 사이클 yield 실측 누적(misskey 14400s/hackerone 43200s 첫 tick).
