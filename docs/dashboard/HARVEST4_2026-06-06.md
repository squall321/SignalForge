# Harvest 4 — Tor·신규 컬렉터·regression 10 (2026-06-06)

## 0. Discovery 요약
- **Tor**: apt 패키지 가용 (0.4.6.10). 컨테이너 내부 미설치·9050 무응답. host 측 systemd 권장. 미설치시 graceful `FMKOREA_USE_PROXY=false` 유지.
- **XDA**: forum.xda-developers.com → xdaforums.com 301, Bunny Shield 403 PoW 차단. 단, `xda-developers.com/tag/samsung/` 200 OK + 기존 `crawler/platforms/xda.py` (141 라인, 5월 15일자) 가 16/20 갤럭시 아이템 정상 파싱. **포럼은 막혔으나 뉴스 태그는 활성** — build 보고가 일부 오도.
- **GSMArena**: samsung-phones-9.php / 모델별 reviews 페이지 전부 200 OK 무차단. user-thread div 안정 markup. 신규 컬렉터 적격.
- **archive**: harvest3p 잔여 폴더 부재 해소 (.sentinel.json 자동 생성), harvest4 sentinel 12:44 생성.
- **regression baseline**: 코드상 11 checks + alembic = 12 (hardware_fr_voc 포함). 라이브 endpoint 무응답 (백엔드 미가동) — 실측은 pytest 25/25 PASS 로 대체.

## 1. H1 archive 자동 생성 + Hook 검증
- `crawler/base/audit.py` (11,949B) `_resolve_archive_dir + _write_sentinel` 추가, `insight/hook_monitor.py` (18,378B) `_existing_archive_rounds` harvest* 패턴 확장.
- `tests/test_audit_archive_auto.py` 3 testcase 신규, 전체 audit/archive regression 19/19 PASS (0.41s).
- `/reports/archive/harvest4/.sentinel.json` (307B, run_id=4268ea467454) 실재. harvest3p 폴더 .sentinel.json 4 related_run_ids 회수.

## 2. H2 FMKorea Tor (Case B — Tor 미가동)
- 9050 무응답 확인 → `FMKOREA_USE_PROXY=false` 유지 (graceful).
- `crawler/tests/test_fmkorea_tor.py` (3,318B) 3 PASS, proxy_pool + tor 합산 12 PASS.
- DB: fmkorea is_active=t, 24h=4, 7d=147 (drift 0%). 본런 INSERT 0 (정책 유지).
- 직접 curl 429 size 3177 — 차단 패턴 변함 없음. 결론: host 측 tor 가동 전까지 동결.

## 3. H3 XDA 컬렉터 (보류·재발견)
- build 보고 "전면 차단" → **부분 오류**. 포럼 (xdaforums.com) 만 PoW 차단, news tag (xda-developers.com/tag/samsung/) 200 OK·기존 `platforms/xda.py` 이미 가동 가능 (mtime 5월 15일).
- pytest xda 전용 테스트 0건 — 회귀 미보장.
- 본런 미실행 (track audit 부재). harvest4 audit 에 H3 미기록.
- **다음 라운드 액션**: news tag 컬렉터 정식 등록 + 단위 테스트 1건 추가.

## 4. H4 GSMArena 컬렉터 신규
- `crawler/platforms/gsmarena_forum.py` (12,515B, 303 라인) 신설, platform id=91 "GSMArena Forum" DB 등록.
- H4 pytest 6/6 PASS, 본런 INSERT **236 건** (audit 12:49 dry → 12:51 preserve, 양 entry 실재).
- `gsmaf_` prefix 236건 — legacy `gsma_` 와 분리 보존. product_id mapped 75/236 (32%, NULL 161건). 자체 보고 "8/8 mapped" 는 distinct product_id 8 의미였음 — drift 표현 모호.

## 5. H5 regression baseline 10 정식 추가
- `backend/app/api/_internal.py` (148,039B) `_HARDWARE_FR_VOC_THRESHOLD=150`, `_BASELINE_HARVEST3P=206` 상수 + check append + docstring "11 + alembic" 모두 grep 실재.
- `backend/tests/test_regression_baseline_10.py` (3,744B) 5 testcase 신설, `test_regression_coverage.py` `_R12_LINEAGE_NAMES` 갱신.
- pytest 18/18 PASS. self-report `current=206` vs curl 직접 측정 동일 — drift 0%.
- ⚠️ 라이브 endpoint 무응답 (백엔드 down) — 본 라운드 실측은 pytest 한정.
- ⚠️ H5 audit JSONL 시작·종료 entry 부재.

## 6. H6 Hardware.fr 보드 + day-4 재평가
- `crawler/platforms/hardware_fr.py` (15,911B, mtime 12:44) 보드 확장, `harvest4_hardware_fr_boards.py` 본런 4회 (12:47/12:54/12:57/13:05 모두 ok, 12:50 1회 error 후 회복).
- 본런 INSERT 누적 **167건**. hardware_fr 24h=375 (실측 검증). voc 보고 127,217 / 실측 127,219 — drift 0.0016%.
- pytest H6 보드 5/5 + 전체 regression 25/25 PASS. day4_spike 보고서 정식 등록.

## 7. 라이브 실측 (2026-06-06)
| 지표 | 값 | 비고 |
|---|---|---|
| voc 누적 | 127,219 | harvest3p 125,952 → +1,267 (H4 236 + H6 167 + 기타) |
| hardware_fr 24h | 375 | day-4 재평가 baseline 206 → +169 |
| gsmaf_ 신규 | 236 | platform_id=91 신설 |
| fmkorea 24h | 4 | Tor 미가동 동결 |
| xda news_tag | 200 OK | 컬렉터 활성, 회귀 미등록 |
| audit harvest4 tracks | H1·H2·H4·H6 | H3·H5 audit entry 부재 |

## 8. 4중 안전장치 + regression
- DRY_RUN → PRESERVE 단계 H4·H6 모두 양 entry 기록.
- ON CONFLICT 보존 정책 유지 (gsmaf_ prefix 충돌 0).
- audit JSONL round=harvest4 10 entries — H1(2) + H2(1) + H4(2) + H6(5).
- Hook archive 자동 검증 통과 (sentinel 0byte 폴더 대응).
- regression pytest 25/25 PASS (코드상 baseline 11 + alembic = 12, hardware_fr_voc 포함). **라이브 endpoint 검증 보류** — 백엔드 가동 후 재확인 필요.

## 9. 잔여 + 다음 라운드
1. **H3 XDA news_tag** 정식 컬렉터 등록 + 단위 테스트 1건 + audit round 기록.
2. **H5 audit entry** 누락 — `_internal.py` 변경분에 audit start/end 호출 wrapper 추가.
3. **백엔드 가동** 후 regression endpoint 라이브 실측 (현재 코드 검증만).
4. **GSMArena product_id NULL 161건** — MODEL_MAP 확장 또는 LLM linker 적용 판단.
5. **FMKorea Tor** — host 측 `sudo apt install tor` 가동 시 즉시 결선 가능.
6. **self-report drift** — H4 "8/8 mapped" 표현 모호 (distinct vs ratio) 표준화 필요.

## 10. 자체 평가
- 신규 데이터 +1,267 (가시적 성장).
- H1 archive 자동화 완성 (이전 라운드 잔여 해소).
- H4 GSMArena 신규 소스 확보 (236건).
- H6 day-4 spike 재평가 + boards 확장 안정.
- **부분 실패**: H3 (재발견 후 미통합), H5 (audit 미기록), Tor (인프라 미가동).
- regression baseline 10→11 (코드 측 가산) — 명칭 정합성 검토 필요.
