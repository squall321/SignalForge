# SignalForge Data Harvest 2 — 2026-06-06

총괄: voc_records **124,969** (+2,492 vs Harvest 1 122,477) / 활성 platforms **67** (65 + resetera + ifixit) / 24h 신규 **10,210**

---

## 1. 트랙별 결과 + verify 요약

| 트랙 | 상태 | 산출 핵심 | verify 결론 | self-report drift |
|-----|-----|---------|------------|-----------------|
| B 한국 페이지네이션 본런 | **partial** | clien=25/dcinside=20/ppomppu=30/fmkorea=20/dogdrip=25 setdefault 확정, audit JSONL 가동 | 프로세스 ALIVE·NLP 단계에서 5분 verify 윈도우 만료 → INSERT 0 시점 캡처. 후속 본런은 Celery beat 정상 회전 중 | runtime drift 추정 0%, INSERT 검증은 후행 |
| C ResetEra + iFixit 신규 | **OK** | 파일 4 신규 + tasks.py/celery_app.py 결선, 13+14 단위 테스트 | 실 INSERT 93건 (resetera 10 + ifixit 83) 확인, 65→67 활성 매칭 | 보고 ifixit=14 vs 실제 16 테스트 (~14% subcomponent drift), 총합 57 일치 |
| D Slack Webhook 결선 | **OK (키 미입력)** | slack_notifier.py 신규, dry-run 18건 라벨 누적, `/alerts/channels` endpoint 응답 정상 | 모든 결선 코드/회귀 7+5+2 PASS. 키 입력 즉시 활성 가능 | 보고 LOC 302 vs 실제 356 (+18% LOC drift, 기능 영향 없음) |
| E Groq 외부 LLM 검증 | **OK (키 미입력)** | groq_health_check.py + cross-check + redact, 3/3 단위 테스트 | 키 미입력 graceful skip 확인 (exit 0) | 0% (수치 일치) |
| F 1주 운영 모니터링 | **OK** | collection_trend.py 모듈 + `/collection-trend?days=N` endpoint, 단위 1 케이스 | HTTP 200, 7일 매트릭스 65 사이트 75,456건, 일평균 10,779건 산출 | 0% (read-only 분석, 쓰기 없음) |

---

## 2. B 한국 페이지네이션 신규 voc (24h)

| 사이트 | 페이지 상한 | 24h 신규 |
|-------|----------|--------|
| dcinside | 20 | 2,176 |
| ppomppu | 30 | 1,469 |
| dogdrip | 25 | 255 |
| clien | 25 | 33 |
| fmkorea | 20 | 4 |

소계 24h **3,937건**. dcinside·ppomppu 깊이 확장 효과 즉시 가시 (이전 12 페이지 운영 대비 ~3-4× 증가). clien/fmkorea 저조 — Cloudflare 차단·게시판 회전 느림 양가능 원인. audit JSONL `audit_korean_deep_harvest2.jsonl` 1주 누적 후 평가.

## 3. C ResetEra / iFixit 첫 수집

- resetera 누적 **10건** (Google News RSS 3쿼리 fan-out, Cloudflare 우회)
- ifixit 누적 **83건** (News RSS + Answers API 7키워드 fan-out + og:description 본문 보강)
- 첫 가동 24h: resetera 10 / ifixit 83 — iFixit 키워드 fan-out 7 효과 즉시 검증
- 활성 platforms 65 → **67** 매칭

## 4. D Slack 채널 결선 (키 입력 가이드)

상태: `enabled:false, dry_run:true, channel:""` — Webhook 미입력
키 입력 절차:
```
backend/.env 에 SLACK_WEBHOOK_URL=https://hooks.slack.com/services/... 추가
docker compose restart backend crawler beat
curl localhost:8000/api/v1/_internal/alerts/channels → enabled:true 확인
```
입력 즉시 alert_events 24h **117건** 자동 dispatch 시작 (Celery beat alert-slack-dispatch-5m, args (50,24))

## 5. E Groq 키 입력 가이드

`.env` 3 슬롯 (현재 모두 비어 있음):
```
EXTERNAL_API_KEY=gsk_...
EXTERNAL_BASE_URL=https://api.groq.com/openai/v1
EXTERNAL_MODEL=llama-3.3-70b-versatile
```
검증: `crawler/.venv/bin/python crawler/scripts/groq_health_check.py --json`
- 미입력: `{"status":"skipped"}` (exit 0)
- 입력 OK: backend `/llm-status?ping=true` 와 `tier_label='external:<model>'` 정확 일치 cross-check

가이드 본문: `/home/koopark/claude/SignalForge/docs/dashboard/LLM_EXTERNAL_GUIDE.md` (5644B, 145 LOC)

## 6. F 1주 운영 모니터링

`GET /api/v1/_internal/collection-trend?days=7` (localhost only) 가동
- 활성 사이트 **65** 매트릭스
- 7일 총 voc **75,456** / 일평균 **10,779**
- daily_totals: 7,853 → 7,998 → 4,765 → **37,135** → 4,806 → 9,304 → 3,595
- day-4 스파이크 (37k) — Harvest 1 백필 잔여 추정, day-7 3,595 저조는 day-1 부분일자 효과

## 7. 가동 절차 + 키 입력 절차

신규 사이트 단위 가동:
```
cd crawler && .venv/bin/celery -A celery_app call tasks.crawl_resetera
cd crawler && .venv/bin/celery -A celery_app call tasks.crawl_ifixit
```
한국 deep harvest 단발:
```
.venv/bin/python crawler/scripts/korean_pagination_deep.py --dry-run
.venv/bin/python crawler/scripts/korean_pagination_deep.py
```
키 입력 후 재기동:
```
docker compose restart backend crawler beat
.venv/bin/python crawler/scripts/groq_health_check.py --json --backend-url http://localhost:8000
```

## 8. 측정 수치 종합 (실 voc_records INSERT N)

| 항목 | Harvest 1 | Harvest 2 | Δ |
|-----|---------|---------|----|
| voc_records 총 | 122,477 | **124,969** | +2,492 |
| 활성 platforms | 65 | **67** | +2 |
| 24h 신규 | 10,134 | **10,210** | +76 |
| 신규 사이트 voc | 0 | **93** (resetera 10 + ifixit 83) | +93 |
| 한국 deep 24h | n/a | **3,937** (dc/ppm/dog/clien/fm) | 신규 측정 |
| alert_events 24h | 43 | **117** | +74 (룰 정착) |
| 외부 LLM 키 | 미입력 | 미입력 (가이드+health check 완비) | 도구 +1 |
| Slack Webhook | 미입력 | 미입력 (결선 완료, dry-run 18 라벨) | 결선 +1 |

## 9. 다음 단계 + 잔여

1. **B verify 후속**: korean_pagination_deep.py 본런 INSERT N 5분 윈도우 만료 → 다음 라운드 audit JSONL 누적 분석 (현재 ALIVE/in-progress 캡처에 그침)
2. **외부 키 2개 입력 대기**: SLACK_WEBHOOK_URL + EXTERNAL_API_KEY (사용자 액션) — 입력 즉시 alert 117/24h + LLM grounding 자동 활성
3. **신규 사이트 추가 후보** (Discovery 결과): Hardware.fr forum (프랑스 IT, ~10-30/day Galaxy), Hardware.fr 본런 시 언어 필터 + ko/en 번역 파이프 선결 필요
4. **clien/fmkorea 저조 원인 분석**: 24h 33/4건 → Cloudflare 차단 vs 게시판 회전 속도 양가능, 1주 audit 후 user-agent 회전·sleep 조정 검토
5. **day-4 스파이크 (37k) 정상 여부**: Harvest 1 백필 잔여 추정 — collection_trend endpoint 1주 누적 후 분류 (steady/swing/down/low) 재평가

verify 부분 실패 (B INSERT N): partial 명시. self-report drift (C ifixit 14→16, D LOC 302→356) 정확 기록. 실 voc INSERT 위주 측정.
